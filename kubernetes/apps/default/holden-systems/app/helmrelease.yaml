---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: holden-systems
spec:
  chartRef:
    kind: OCIRepository
    name: holden-systems
  interval: 1h
  values:
    controllers:
      holden-systems:
        replicas: 2 # zero-downtime rollouts and survives single-pod restart
        strategy: RollingUpdate
        rollingUpdate:
          maxUnavailable: 0
          maxSurge: 1
        containers:
          app:
            image:
              repository: ghcr.io/andrei-iacobb/holden-systems-site
              # Next.js 16.3 standalone image built from the merged app commit.
              tag: 27c564e@sha256:342d0ee165752b0ec3261fe2fc84cb826f654555249db3a329c808fe2b0af9c9
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz/
                    port: &port 8080
                  initialDelaySeconds: 5
                  periodSeconds: 10
                  timeoutSeconds: 2
                  failureThreshold: 3
              readiness: *probes
            resources:
              requests:
                cpu: 5m
                memory: 64Mi
              limits:
                memory: 128Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop: ["ALL"]
    defaultPodOptions:
      imagePullSecrets:
        - name: ghcr-pull-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: holden-systems
    service:
      app:
        controller: holden-systems
        ports:
          http:
            port: *port
    persistence:
      # Keep the standalone Node runtime's temporary path writable while the
      # image root remains read-only.
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
