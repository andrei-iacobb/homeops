---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: minecraft-bedrock
spec:
  interval: 1h
  path: ./kubernetes/apps/default/minecraft-bedrock/app
  postBuild:
    substitute:
      APP: minecraft-bedrock
      APP_PVC: minecraft-bedrock
      VOLSYNC_SCHEDULE: "50 3 * * *"
      MOVER_UID: "1000"
      MOVER_GID: "1000"
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: volsync-secret
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: default
  wait: false
