---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: it-tools
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - it-tools.iacob.uk
  rules:
    - backendRefs:
        - name: it-tools
          port: 80
