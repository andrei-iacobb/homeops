---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: stirling-pdf
spec:
  interval: 1h
  path: ./kubernetes/apps/default/stirling-pdf/app
  postBuild:
    substitute:
      APP: stirling-pdf
      APP_PVC: stirling-pdf
      VOLSYNC_SCHEDULE: "45 3 * * *"
      MOVER_UID: "0"
      MOVER_GID: "0"
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: volsync-secret
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: default
  wait: false
