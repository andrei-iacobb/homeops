---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: beta
  namespace: code
spec:
  targetNamespace: code
  commonMetadata:
    labels:
      app.kubernetes.io/name: beta
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  prune: true
  wait: false
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./kubernetes/apps/code/beta/app
  postBuild:
    substitute:
      APP: beta
      APP_PVC: beta-home
      VOLSYNC_SCHEDULE: "20 3 * * *"
      MOVER_UID: "1000"
      MOVER_GID: "1000"
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: volsync-secret
        kind: Secret
