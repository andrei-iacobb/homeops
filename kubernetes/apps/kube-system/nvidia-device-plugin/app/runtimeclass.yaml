---
# GPU pods set runtimeClassName: nvidia so containerd on the gpu-worker node
# uses the nvidia-container-runtime shipped by the Talos nvidia-container-toolkit extension.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
