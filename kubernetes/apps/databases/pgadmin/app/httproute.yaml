---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pgadmin
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - pgadmin.iacob.uk
  rules:
    - backendRefs:
        - name: pgadmin
          port: 80
