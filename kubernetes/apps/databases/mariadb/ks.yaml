---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mariadb
spec:
  interval: 1h
  path: ./kubernetes/apps/databases/mariadb/app
  postBuild:
    substitute:
      APP: mariadb
      APP_PVC: mariadb
      MOVER_UID: "999"
      MOVER_GID: "999"
      VOLSYNC_SCHEDULE: "5 */6 * * *"
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: volsync-secret
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: databases
  wait: false
