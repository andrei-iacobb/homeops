---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ollama
spec:
  chartRef:
    kind: OCIRepository
    name: ollama
  interval: 1h
  values:
    controllers:
      ollama:
        strategy: Recreate
        containers:
          app:
            image:
              repository: ollama/ollama
              tag: 0.33.1
            env:
              OLLAMA_CONTEXT_LENGTH: "32768"
              OLLAMA_LOAD_TIMEOUT: "10m"
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/tags
                    port: 11434
                  initialDelaySeconds: 60
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: 2000m
                memory: 8Gi
              limits:
                # Raised from 8 cores: Ollama sizes its thread pool from the
                # host's 32 cores and ignores the cgroup quota, so a low limit
                # thrashed (measured 0.8 tok/s vs 11 tok/s at 16). Callers
                # should also pass num_thread matching this value.
                cpu: 16000m
                memory: 96Gi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: false
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
    service:
      app:
        ports:
          http:
            port: 11434
    persistence:
      data:
        type: persistentVolumeClaim
        storageClass: openebs-hostpath
        size: 100Gi
        accessMode: ReadWriteOnce
        globalMounts:
          - path: /home/ubuntu/.ollama
