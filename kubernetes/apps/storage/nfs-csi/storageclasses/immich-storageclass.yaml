---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-immich
provisioner: cluster.local/nfs-subdir-external-provisioner-immich
parameters:
  pathPattern: "${.PVC.namespace}-${.PVC.name}"
  onDelete: retain
allowVolumeExpansion: true
volumeBindingMode: Immediate
reclaimPolicy: Retain
