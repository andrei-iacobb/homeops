---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openebs

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./openebs/ks.yaml
  - ./nfs-csi/ks.yaml
  - ./nfs-csi-immich/ks.yaml
  - ./nfs-csi/storageclasses/ks.yaml
  # TODO: Re-enable volsync when backup permission issues are resolved
  # - ./volsync/ks.yaml
