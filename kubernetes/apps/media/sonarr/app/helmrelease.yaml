---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sonarr
spec:
  chartRef:
    kind: OCIRepository
    name: sonarr
  interval: 1h
  values:
    controllers:
      sonarr:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: lscr.io/linuxserver/sonarr
              tag: latest
            env:
              PUID:
                valueFrom:
                  secretKeyRef:
                    name: sonarr-secret
                    key: PUID
              PGID:
                valueFrom:
                  secretKeyRef:
                    name: sonarr-secret
                    key: PGID
              TZ:
                valueFrom:
                  secretKeyRef:
                    name: sonarr-secret
                    key: TZ
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/v3/system/status
                    port: 8989
                  initialDelaySeconds: 60
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: true
              readOnlyRootFilesystem: false
              capabilities:
                drop: ["ALL"]
                add: ["CHOWN", "SETUID", "SETGID", "DAC_OVERRIDE", "FOWNER"]
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                cpu: 2000m
                memory: 2Gi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: false
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 1000
        seccompProfile:
          type: Unconfined
    service:
      app:
        ports:
          http:
            port: 8989
    persistence:
      config:
        type: persistentVolumeClaim
        storageClass: openebs-hostpath
        size: 5Gi
        accessMode: ReadWriteOnce
        globalMounts:
          - path: /config
      downloads:
        type: persistentVolumeClaim
        existingClaim: sonarr-downloads
        globalMounts:
          - path: /downloads
      movies:
        type: persistentVolumeClaim
        existingClaim: sonarr-movies
        globalMounts:
          - path: /movies
      tv:
        type: persistentVolumeClaim
        existingClaim: sonarr-tv
        globalMounts:
          - path: /tv
      music:
        type: persistentVolumeClaim
        existingClaim: sonarr-music
        globalMounts:
          - path: /music
      books:
        type: persistentVolumeClaim
        existingClaim: sonarr-books
        globalMounts:
          - path: /books
    route:
      app:
        hostnames: ["sonarr.iacob.uk"]
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: 8989
