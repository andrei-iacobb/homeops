---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: readarr
spec:
  interval: 1h
  path: ./kubernetes/apps/media/readarr/app
  postBuild:
    substitute:
      APP: readarr
      APP_PVC: readarr
      VOLSYNC_SCHEDULE: "10 3 * * *"
      MOVER_UID: "568"
      MOVER_GID: "568"
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: volsync-secret
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: false
