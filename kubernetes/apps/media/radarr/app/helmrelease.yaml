---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: radarr
spec:
  chartRef:
    kind: OCIRepository
    name: radarr
  interval: 1h
  values:
    controllers:
      radarr:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: lscr.io/linuxserver/radarr
              tag: latest
            env:
              PUID:
                valueFrom:
                  secretKeyRef:
                    name: radarr-secret
                    key: PUID
              PGID:
                valueFrom:
                  secretKeyRef:
                    name: radarr-secret
                    key: PGID
              TZ:
                valueFrom:
                  secretKeyRef:
                    name: radarr-secret
                    key: TZ
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/v3/system/status
                    port: 7878
                  initialDelaySeconds: 60
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                cpu: 2000m
                memory: 2Gi
        additionalVolumes:
          - name: downloads
            persistentVolumeClaim:
              claimName: radarr-downloads
          - name: movies
            persistentVolumeClaim:
              claimName: radarr-movies
          - name: tv
            persistentVolumeClaim:
              claimName: radarr-tv
          - name: music
            persistentVolumeClaim:
              claimName: radarr-music
          - name: books
            persistentVolumeClaim:
              claimName: radarr-books
        additionalVolumeMounts:
          - name: downloads
            mountPath: /downloads
          - name: movies
            mountPath: /movies
          - name: tv
            mountPath: /tv
          - name: music
            mountPath: /music
          - name: books
            mountPath: /books
    defaultPodOptions:
      securityContext:
        runAsNonRoot: false
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
    service:
      app:
        ports:
          http:
            port: 7878
    persistence:
      config:
        enabled: true
        storageClass: openebs-hostpath
        size: 5Gi
        accessModes:
          - ReadWriteOnce
    route:
      app:
        hostnames: ["radarr.iacob.uk"]
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: 7878
