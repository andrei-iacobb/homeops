---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: agregarr
spec:
  interval: 1h
  path: ./kubernetes/apps/media/agregarr/app
  postBuild:
    substitute:
      APP: agregarr
      APP_PVC: agregarr
      VOLSYNC_SCHEDULE: "0 2 * * *"
      MOVER_UID: "1000"
      MOVER_GID: "1000"
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: volsync-secret
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: false
