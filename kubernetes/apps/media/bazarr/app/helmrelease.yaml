---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bazarr
spec:
  chartRef:
    kind: OCIRepository
    name: bazarr
  interval: 1h
  values:
    controllers:
      bazarr:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: lscr.io/linuxserver/bazarr
              tag: latest
            env:
              PUID:
                valueFrom:
                  secretKeyRef:
                    name: bazarr-secret
                    key: PUID
              PGID:
                valueFrom:
                  secretKeyRef:
                    name: bazarr-secret
                    key: PGID
              TZ:
                valueFrom:
                  secretKeyRef:
                    name: bazarr-secret
                    key: TZ
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/system/status
                    port: 6767
                  initialDelaySeconds: 60
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                cpu: 2000m
                memory: 2Gi
        additionalVolumes:
          - name: movies
            persistentVolumeClaim:
              claimName: bazarr-movies
          - name: tv
            persistentVolumeClaim:
              claimName: bazarr-tv
          - name: music
            persistentVolumeClaim:
              claimName: bazarr-music
          - name: books
            persistentVolumeClaim:
              claimName: bazarr-books
        additionalVolumeMounts:
          - name: movies
            mountPath: /movies
          - name: tv
            mountPath: /tv
          - name: music
            mountPath: /music
          - name: books
            mountPath: /books
    defaultPodOptions:
      securityContext:
        runAsNonRoot: false
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
    service:
      app:
        ports:
          http:
            port: 6767
    persistence:
      config:
        enabled: true
        storageClass: openebs-hostpath
        size: 5Gi
        accessModes:
          - ReadWriteOnce
    route:
      app:
        hostnames: ["bazarr.iacob.uk"]
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: 6767
