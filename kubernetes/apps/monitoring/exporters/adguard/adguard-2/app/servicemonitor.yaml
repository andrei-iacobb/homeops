---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: adguard-exporter-2
spec:
  selector:
    matchLabels:
      app: adguard-exporter-2
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics
