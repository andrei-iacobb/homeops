---
apiVersion: v1
kind: Service
metadata:
  name: wireguard-exporter
  labels:
    app: wireguard-exporter
spec:
  ports:
  - name: metrics
    port: 9586
    targetPort: metrics
  selector:
    app: wireguard-exporter
