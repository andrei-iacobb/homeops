---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: wireguard-exporter
spec:
  selector:
    matchLabels:
      app: wireguard-exporter
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics
