---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: truenas-exporter
  labels:
    app: truenas-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: truenas-exporter
  template:
    metadata:
      labels:
        app: truenas-exporter
    spec:
      containers:
      - name: exporter
        image: prom/graphite-exporter:latest
        args:
          - --web.listen-address=:9108
        ports:
        - name: metrics
          containerPort: 9108
        - name: graphite
          containerPort: 9109
          protocol: UDP
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
