---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: truenas-exporter
spec:
  selector:
    matchLabels:
      app: truenas-exporter
  endpoints:
    - port: metrics
      interval: 15s
    interval: 30s
    path: /metrics
