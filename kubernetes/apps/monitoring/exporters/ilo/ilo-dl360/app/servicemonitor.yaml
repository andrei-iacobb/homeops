---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ilo-exporter-dl360
spec:
  selector:
    matchLabels:
      app: ilo-exporter-dl360
  endpoints:
  - port: metrics
    interval: 60s
    path: /metrics
    params:
      hosts:
      - "192.168.1.175"
