---
# TODO: Rebuild monitoring stack properly:
# - Disable Grafana sidecar, use static dashboard provisioning (DavidIlie pattern)
#   OR migrate to Grafana Operator (onedr0p pattern)
# - Sidecar (kiwigrid/k8s-sidecar) OOMs at 128Mi default, causes kernel memory
#   reclaim storms that take down the entire single-node cluster for hours
# - Audit dashboards: adguard, truenas, proxmox, ilo - which are still useful?
# - Consider standalone Grafana instead of bundled kube-prometheus-stack
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # - prometheus/ks.yaml
  # - grafana/ks.yaml
  # - exporters/ks.yaml
  # - graphite-exporter/ks.yaml
  # - loki/ks.yaml
  # - promtail/ks.yaml
