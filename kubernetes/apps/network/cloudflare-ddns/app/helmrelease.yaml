---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudflare-ddns
spec:
  chartRef:
    kind: OCIRepository
    name: cloudflare-ddns
  interval: 1h
  values:
    controllers:
      cloudflare-ddns:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: docker.io/favonia/cloudflare-ddns
              tag: 1.16.2
            env:
              # plex.iacob.co.uk is now fronted by the OCI VPS — keep it out of DDNS so home WAN IP isn't published
              DOMAINS: "wg.iacob.co.uk"
              PROXIED: "false"
              IP6_PROVIDER: "none"
              UPDATE_CRON: "@every 5m"
            envFrom:
              - secretRef:
                  name: cloudflare-ddns-secret
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: 5m
                memory: 32Mi
              limits:
                cpu: 100m
                memory: 64Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
