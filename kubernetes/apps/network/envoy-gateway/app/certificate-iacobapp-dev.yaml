---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: iacobapp-dev-production
spec:
  secretName: iacobapp-dev-production-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "iacobapp.dev"
  dnsNames: ["iacobapp.dev", "*.iacobapp.dev"]
