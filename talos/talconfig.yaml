---
clusterName: kubernetes

talosVersion: "${talosVersion}"
kubernetesVersion: "${kubernetesVersion}"

endpoint: https://192.168.1.185:6443
additionalApiServerCertSans: &sans
  - "127.0.0.1"
  - "192.168.1.185"
additionalMachineCertSans: *sans

clusterPodNets: ["10.67.0.0/16"]
clusterSvcNets: ["10.41.0.0/16"]

# Disable built-in CNI to use Cilium
cniConfig:
  name: none

nodes:
  - hostname: "home-cluster"
    ipAddress: "192.168.1.185"
    installDisk: "/dev/vda"
    machineSpec:
      secureboot: false
    talosImageURL: factory.talos.dev/installer/ce4c980550dd2ab1b17bbf2b08801c7eb59418eafe8f279833297925d67c7515
    controlPlane: true
    networkInterfaces:
      # Main LAN - default route for API, ping, etc.
      - interface: "ens18"
        dhcp: false
        addresses:
          - "192.168.1.185/24"
        routes:
          - gateway: "192.168.1.254"
            network: 0.0.0.0/0
        mtu: 1500
      # ens19: dedicated 10G p2p NFS link to TrueNAS - no default route.
      # TrueNAS p2p IP: 10.0.0.1. Update NFS server in nfs-csi helmrelease to 10.0.0.1 when ready.
      - interface: "ens19"
        dhcp: false
        addresses:
          - "10.0.0.2/30"
        mtu: 9000

# Global patches
patches:
  - "@./patches/global/machine-files.yaml"
  - "@./patches/global/machine-kubelet.yaml"
  - "@./patches/global/machine-network.yaml"
  - "@./patches/global/machine-sysctls.yaml"
  - "@./patches/global/machine-time.yaml"

# Controller patches
controlPlane:
  patches:
    - "@./patches/controller/cluster.yaml"

